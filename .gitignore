# Vendored upstream (pinned + re-fetchable via deploy/bootstrap.sh)
deploy/.netbox-docker/

# Secrets / local env
*.env
!*.env.example
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store

# AI ingest — local-only identifier mapping (reversal key; never commit)
ingest/*.map.json
