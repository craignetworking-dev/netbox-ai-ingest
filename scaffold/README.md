# Layer 2 — Data-model scaffold (planned)

Opinionated baseline NetBox objects: sites, device roles, device types, interface templates.
Built as idempotent, version-controlled definitions so any deployment lands the same way.
